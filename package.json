{
  "name": "production-project",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "rspack serve --env mode=development",
    "build": "rspack build --env mode=production",
    "build:dev": "rspack build --env mode=development",
    "build:analyze": "rspack build --env mode=production --env analyze=true",
    "start": "rspack serve",
    "type-check": "tsc --noEmit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@rspack/cli": "^1.4.8",
    "@rspack/core": "^1.4.8",
    "@swc/register": "^0.1.10",
    "@types/node": "^24.2.0",
    "typescript": "^5.9.2"
  }
}
